{
  "name": "trunk-list-demo-db-pixi-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:index": "node scripts/generate-demo-asset-index.mjs",
    "dev": "npm run generate:index && vite",
    "demo:db": "npm run generate:index && vite",
    "build": "npm run generate:index && tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "pixi.js": "^8.6.6"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
