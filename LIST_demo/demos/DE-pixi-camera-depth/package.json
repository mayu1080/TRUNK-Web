{
  "name": "trunk-list-demo-de-pixi-camera-depth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:index": "npm run generate:index --prefix ../DB-pixi-core && node scripts/copy-demo-index.mjs",
    "dev": "npm run generate:index && vite",
    "demo:de": "npm run generate:index && vite",
    "build": "npm run generate:index && tsc -p ../DD-pixi-three-style/tsconfig.json && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "framer-motion": "^11.15.0",
    "pixi.js": "^8.6.6",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
