# 8/20 color variant（白黒なし）

`demo-0820` の複製。**本体の 8/20（白黒 + Bubble Color Reveal）は変更しない。**

違いはカードの白黒シェーダを付けないことだけ。LIST / Bubble 円 / ZOOM / Drawer / DETAIL / Dolly / ロゴはそのまま。

## 起動（app/ から）

```powershell
cd app
npm run start:0820-color
```

またはリポジトリ直下 `launch-0820-color.bat`。

当日の白黒デモは従来どおり `npm run start:0820`。
